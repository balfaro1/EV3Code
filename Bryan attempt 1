brick.SetColorMode(1, 4);
color_rgb = brick.ColorRGB(1);
while 1
if (color_rgb(1) > 40 && color_rgb(1)<50 )
    {brick.MoveMotor('AD',100);}
else if (color_rgb(1) > 15 && color_rgb(1)<25 )
    {brick.MoveMotor('AD',0);}
    end
end

end
